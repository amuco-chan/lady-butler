{
  "openapi": "3.1.0",
  "info": {
    "title": "Lady Butler GPT Inbox",
    "description": "Send task and calendar event candidates from a Custom GPT conversation to the Lady Butler app. The API returns an import URL; the user must open it and confirm before anything is saved.",
    "version": "1.2.0"
  },
  "servers": [
    {
      "url": "https://lady-butler.vercel.app"
    }
  ],
  "paths": {
    "/api/gpt-inbox": {
      "post": {
        "operationId": "sendCandidatesToLadyButler",
        "summary": "Send task or calendar candidates to Lady Butler",
        "description": "Send candidates only after distinguishing them by purpose. A task is something the user completes and checks off, including assignments, submissions, errands, purchases, and messages. A task stays type task even when its deadline includes a clock time, such as 'Friday by 18:00'; put that time in deadline and never in startAt. An event is time the user attends or reserves on a calendar, including classes, shifts, appointments, meetings, and travel with a known start time; put that time in startAt. If the user says 'submit my shift preferences', that is a task, while 'work a shift from 18:00' is an event. Do not use a category named 予定. Never invent a date or time. If the distinction or required event start time is unclear, ask one short clarification before calling this action. Return the importUrl and ask the user to open it to review the candidates.",
        "requestBody": {
          "required": true,
          "content": {
            "application/json": {
              "schema": {
                "type": "object",
                "properties": {
                  "sourceText": {
                    "type": "string",
                    "description": "Short quote or paraphrase of what the user said."
                  },
                  "items": {
                    "type": "array",
                    "minItems": 1,
                    "description": "Preferred input. Every candidate must explicitly declare type task or event.",
                    "items": {
                      "type": "object",
                      "properties": {
                        "type": {
                          "type": "string",
                          "enum": ["task", "event"],
                          "description": "Use task for a completable/checkable action, even if its deadline has a time. Use event only for calendar time with a known start."
                        },
                        "title": {
                          "type": "string",
                          "description": "Short title in Japanese."
                        },
                        "deadline": {
                          "type": "string",
                          "description": "Task only. Deadline in ISO format. A phrase such as '18:00までに' belongs here, not in startAt. If unknown, omit it and mention the uncertainty in memo."
                        },
                        "startAt": {
                          "type": "string",
                          "description": "Event only. Known start date/time in ISO-like local format, such as 2026-06-25T14:00. Never use a task deadline here."
                        },
                        "endAt": {
                          "type": "string",
                          "description": "For event items only. End date/time in ISO-like local format. If unknown, omit it and the app will use about one hour."
                        },
                        "location": {
                          "type": "string",
                          "description": "Event only. Place, room, station, URL, or online location."
                        },
                        "category": {
                          "type": "string",
                          "enum": ["課題", "授業", "生活", "バイト", "買い物", "その他"],
                          "description": "Task only. Do not use 予定; calendar entries must use type event instead."
                        },
                        "priority": {
                          "type": "string",
                          "enum": ["高", "中", "低"]
                        },
                        "estimatedMinutes": {
                          "type": "integer",
                          "minimum": 5,
                          "maximum": 720,
                          "description": "Rough effort estimate in minutes."
                        },
                        "memo": {
                          "type": "string",
                          "description": "Useful note such as class name, report condition, first step, location details, or uncertainty."
                        }
                      },
                      "required": ["type", "title"]
                    }
                  },
                  "events": {
                    "type": "array",
                    "description": "Compatibility input for calendar events only. Each event must include a known startAt. Ask the user when the start time is unknown; never invent it.",
                    "items": {
                      "type": "object",
                      "properties": {
                        "title": { "type": "string" },
                        "startAt": { "type": "string" },
                        "endAt": { "type": "string" },
                        "location": { "type": "string" },
                        "memo": { "type": "string" }
                      },
                      "required": ["title", "startAt"]
                    }
                  }
                },
                "anyOf": [
                  { "required": ["items"] },
                  { "required": ["events"] }
                ]
              }
            }
          }
        },
        "responses": {
          "200": {
            "description": "Import URL generated",
            "content": {
              "application/json": {
                "schema": {
                  "type": "object",
                  "properties": {
                    "ok": { "type": "boolean" },
                    "count": { "type": "integer" },
                    "importUrl": {
                      "type": "string",
                      "description": "Open this URL to import the candidates into Lady Butler."
                    },
                    "message": { "type": "string" }
                  }
                }
              }
            }
          }
        }
      }
    }
  }
}
